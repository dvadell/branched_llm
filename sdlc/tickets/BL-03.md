----
title: Cleanup leftovers + configurable timeout on the synchronous Chat API
----

Two small but real rough edges for downstream consumers (notably `soundai`):

## Scope

### 1. Remove the `:cara` leftover in `ChatClient.default_model/0`

`lib/branched_llm/chat_client.ex` line ~85 reads:

```elixir
Application.get_env(:cara, :ai_model) ||
  Application.get_env(:branched_llm, :ai_model, "ollama:cara-cpu")
```

The `:cara` app is a previous incarnation of this project and no longer exists.
Reading it produces a compile/runtime warning for consumers and is dead config.
Replace with a single lookup under `:branched_llm`:

```elixir
Application.get_env(:branched_llm, :ai_model, "ollama:cara-cpu")
```

Update the module docs (`Chat`, `ChatClient`, `BranchedLLM`, README, guides) if
they mention `:cara`. Bump the fallback model string to something neutral, e.g.
`"openai:gpt-4o-mini"`, or keep the current value — decide and document.

### 2. Configurable timeout on `Chat.send_message/3`

`Chat.send_message/3` blocks in `wait_for_sync_result/4` with a hard-coded
`60_000` ms `after` (two clauses). Web apps need to budget the HTTP request
time and fail fast with a friendly message.

- Accept a `:timeout` option (milliseconds, default `60_000`) in `send_message/3`
  and thread it into both `wait_for_sync_result` clauses.
- On timeout keep returning `{:error, "Timed out waiting for LLM response"}`
  (include the timeout value for debuggability).
- Document the option in the `@doc` and the README Quick Start.

Also verify the timeout is actually honored end-to-end: the orchestrator's
outer `retry` loop (10 attempts) runs inside the task, and the sync caller only
waits on `{:llm_end, ...}` / `{:llm_error, ...}`; a slow/never-responding
provider should surface as a timeout within the requested budget, not 60 s.

### 3. Consistency

- Grep the repo for other hard-coded `60_000` / `:cara` references and address
  them (e.g. README examples, `guides/`, tests that assert the 60 s value).

## Acceptance criteria

- [x] No `:cara` references remain in `lib/` (grep clean).
- [x] `Chat.send_message(msg, context, timeout: 5_000)` returns
      `{:error, ...}` within ~5 s against a non-responding provider.
- [x] Default behavior is unchanged (no `timeout:` opt → 60 s).
- [x] Docs/guides updated; `MIX_ENV=test mix test` green (after BL-01).

## Implementation notes

### 1. `:cara` removal

- `ChatClient.default_model/0` now reads only `:branched_llm, :ai_model`,
  falling back to `"openai:gpt-4o-mini"` (neutral default; matches the
  `:openai` default provider). The `:cara` lookup and `ollama:cara-cpu` default
  are gone.
- Updated all config examples mentioning `ollama:cara-cpu` → `openai:gpt-4o-mini`
  (`BranchedLLM`, `Chat`, `ChatClient` moduledocs, `README.md`,
  `guides/getting_started.md`).
- `test/unit/chat_client_test.exs`: the "reads from :cara config as fallback"
  test was replaced with a "falls back to the default model" test.

### 2. Configurable timeout

- `Chat.send_message/3` accepts `:timeout` (ms, default `60_000`), threaded
  into both `wait_for_sync_result/5` clauses. On timeout it returns
  `{:error, "Timed out waiting for LLM response after #{timeout}ms"}`.
- The orchestrator's outer retry loop still runs inside the supervised task; the
  sync caller only waits on `{:llm_end, ...}` / `{:llm_error, ...}`, so a slow
  provider surfaces as a timeout within the requested budget.
- `test/unit/chat_test.exs`: a raw TCP "hang server" (Bypass can't model a
  never-returning handler — its expectation verification fails teardown with
  "No HTTP request arrived") accepts the connection and never responds; the
  test asserts `send_message(timeout: 1_000)` returns the timeout error fast.

### 3. Consistency

- `guides/tutorial_iex.md`: the "60-second timeout" warning now documents the
  configurable `:timeout` option.
- `README.md` Quick Start documents the `:timeout` option.
- `grep -rn ":cara" lib test guides README.md` → clean.
- Hard-coded `60_000` remains only as the documented default in `chat.ex`;
  `@tag timeout: 60_000` in e2e tests are ExUnit test timeouts, not the API.

## Verification

- `grep -rn ":cara" lib test guides README.md` → no matches.
- A unit test (raw TCP hang server) that never sends the SSE terminator and
  asserts the caller returns `{:error, ...}` within the configured timeout.
- `mix precommit` → exit 0 (compile --warnings-as-errors, format, credo
  --strict, dialyzer 0 errors, test --cover 95.79%).